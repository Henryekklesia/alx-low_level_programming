#include "main.h"

/**
 * print_alphabet - function name for the ptototype
 * Return: returns void
 */

void print_alphabet(void)
{
	char alpha;
	for (alpha = 'a'; alpha <= 'z'; alpha++)
		_putchar(alpha);
	_putchar('\n');
}
