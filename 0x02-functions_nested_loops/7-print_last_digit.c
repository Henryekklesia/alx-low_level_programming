#include "main.h"
/**
 * print_last_digit - Prints last digit
 * @d: digit printed.
 * Return: Always 0
 */
int print_last_digit(int d)
{
	int digit = d % 10;

		digit = -digit;
}
_putchar(digit);
return (digit);
