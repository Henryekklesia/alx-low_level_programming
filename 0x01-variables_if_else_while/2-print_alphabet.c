#include <stdio.h>

/**
 * main - This is the main function
 * Return: always 0 (success)
 */

int main(void)
{

	for (char x = 'a'; x <= 'z'; x++)
		putchar(x);

	for (char y = 'A'; y <= 'Z'; y++)
		putchar(y);

return (0);
}
